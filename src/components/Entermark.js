/* function Entermark(props)
{
       function clickhandlar()
       {
        let x=props.Fecthfirstvalue;
        let y=props.Fecthsecondtvalue;
        let mark=[];
        mark.push(x);
        let credit=[];
        credit.push(y);
        props.Emptyvalue();
        console.log(mark[0]);
        console.log(credit[0]);
        console.log(mark.length);
        console.log(x);
       }

       return(
           <button onClick={clickhandlar}>Next</button>
       );
} */
